# Presentation Review

## 指摘内容
| No. | 観点 | 対象 | 優先度 | 指摘内容 | 推奨対応 | 状況 | 備考 |
|---|---|---|---|---|---|---|---|
| 1 | 不変条件・境界値 / 一貫性 | `src/main/java/jp/i432kg/footprint/presentation/api/request/PostRequest.java`<br>`src/main/java/jp/i432kg/footprint/presentation/api/request/ReplyRequest.java`<br>`src/main/java/jp/i432kg/footprint/presentation/api/request/SignUpRequest.java`<br>`src/main/java/jp/i432kg/footprint/presentation/api/PostRestController.java`<br>`src/main/java/jp/i432kg/footprint/presentation/api/ReplyRestController.java`<br>`src/main/java/jp/i432kg/footprint/presentation/api/UserRestController.java` | Medium | HTTP 境界の Bean Validation がドメイン制約に追いついていない。`PostRequest.comment` は未指定でも通るが controller では必ず `Comment.of(...)` を呼ぶため、入力不備が presentation ではなく domain 例外へ流れる。`SignUpRequest` も `userName` が `@Size(max = 20)` だけで最小長や形式が抜けており、`birthDate` も `@Past` のみで未指定を拒否していないため、公開 API の入力契約が実装とずれている。 | request DTO の Bean Validation を、少なくとも「必須かどうか」と「明らかな長さ制約」が controller 境界で完結する形へ揃える。ドメインと完全重複させる必要はないが、presentation が受け付ける入力と実際に処理可能な入力は一致させる。 | クローズ | request DTO は `PostComment` / `ReplyComment` / `UserName` / `RawPassword` / `BirthDate` の制約に寄せて更新済み。path/query も `@Validated` で ULID・検索キーワード・座標範囲を presentation 境界で検証し、値オブジェクト直受け用 converter は削除済み。投稿コメントは任意仕様に合わせて controller で `null` を空文字へ正規化している。 |
| 2 | 一貫性 / 不変条件・境界値 | `src/main/java/jp/i432kg/footprint/presentation/api/response/mapper/PostResponseMapper.java`<br>`src/main/java/jp/i432kg/footprint/presentation/api/response/mapper/ReplyResponseMapper.java`<br>`src/main/java/jp/i432kg/footprint/presentation/api/response/mapper/UserProfileResponseMapper.java`<br>`src/main/java/jp/i432kg/footprint/presentation/helper/ImageUrlResolver.java`<br>`src/main/java/jp/i432kg/footprint/presentation/helper/LocalImageUrlResolver.java`<br>`src/main/java/jp/i432kg/footprint/presentation/helper/S3ImageUrlResolver.java` | Medium | `presentation.api` / `presentation.api.response` / `presentation.api.response.mapper` / `presentation.helper` は `@NullMarked` 配下だが、実際には public API で `null` を受け入れたり返したりしている。response mapper は `orElse(null)` を返し、converter は空入力時に `null` を返し、`ImageUrlResolver` 実装も `storageObject == null` で `null` を返す。静的 nullability 契約と実装が食い違っており、読み手にも IDE にも誤った前提を与える。 | `@Nullable` を付けて契約を実装に合わせるか、public API から `null` 許容をなくして controller / caller 側で空値処理を閉じる。以前の application 層レビューと同じく、「実際に `null` が流れる境界は明示する」方針に揃える。 | クローズ | `adr_nullable_boundary_policy.md` の方針に合わせ、`presentation` 配下の `package-info.java` から `@NullMarked` を外した。framework 境界が多い層では package 単位の non-null default を使わず、必要箇所だけ個別に扱う方針へ統一した。値オブジェクト直受け用 converter は controller 方針変更に伴い削除済み。 |
