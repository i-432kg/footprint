# Review Check for No.4 and No.5

## Conclusion

- No.4 の対応は妥当です。画像保存とメタデータ抽出の技術詳細は `domain` から `application` のポートへ移されており、元指摘の論点は解消されています。[ImageStorage.java](/Users/432kg/IdeaProjects/footprint/src/main/java/jp/i432kg/footprint/application/port/ImageStorage.java#L17) [ImageMetadataExtractor.java](/Users/432kg/IdeaProjects/footprint/src/main/java/jp/i432kg/footprint/application/port/ImageMetadataExtractor.java#L15)
- No.5 についても、`adr_nullable_boundary_policy.md` の方針を前提にするなら、factory に `null` チェックを足さない判断は妥当です。`domain.model` は `@NullMarked` 配下で non-null 前提、`null` の吸収は boundary 側の責務として整理されています。[adr_nullable_boundary_policy.md](/Users/432kg/IdeaProjects/footprint/docs/adr/adr_nullable_boundary_policy.md)

## Assessment

- `Location.unknown()` の導入により、画像の位置情報不在を `null` ではなく明示的な状態で表現できています。[Location.java](/Users/432kg/IdeaProjects/footprint/src/main/java/jp/i432kg/footprint/domain/model/Location.java#L16) [Image.java](/Users/432kg/IdeaProjects/footprint/src/main/java/jp/i432kg/footprint/domain/model/Image.java#L107)
- `ParentReply.root()` の導入により、返信の親なし状態も `null` から状態オブジェクトへ移せています。[ParentReply.java](/Users/432kg/IdeaProjects/footprint/src/main/java/jp/i432kg/footprint/domain/model/ParentReply.java#L44)
- boundary 側でも、画像メタデータ抽出時に GPS 不在を `Location.unknown()` へ変換してから domain に渡しており、ADR の責務分担と一致しています。[LocalImageRepositoryImpl.java](/Users/432kg/IdeaProjects/footprint/src/main/java/jp/i432kg/footprint/infrastructure/storage/repository/LocalImageRepositoryImpl.java#L72) [S3ImageRepositoryImpl.java](/Users/432kg/IdeaProjects/footprint/src/main/java/jp/i432kg/footprint/infrastructure/storage/repository/S3ImageRepositoryImpl.java#L94)

## Follow-up

- No.5 の完了条件は「factory に `null` チェックを入れること」ではなく、「boundary で `null` を吸収し、domain へ `null` を流さないこと」に読み替えるのが適切です。
- 今後の確認観点としては、domain model に public builder などの別生成経路がある場合に、factory を経由せず不変条件を破れないかを別途見るのがよいです。今回の No.4 / No.5 の範囲では、直ちに差し戻すべき問題は見当たりません。

## Verification

- 実施: `./gradlew test --tests 'jp.i432kg.footprint.application.command.PostCommandServiceTest' --tests 'jp.i432kg.footprint.domain.model.ImageTest' --tests 'jp.i432kg.footprint.domain.model.LocationTest' --tests 'jp.i432kg.footprint.domain.model.PostTest' --tests 'jp.i432kg.footprint.domain.model.UserTest'`
- 結果: 成功
