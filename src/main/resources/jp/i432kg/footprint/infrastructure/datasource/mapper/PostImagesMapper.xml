<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="jp.i432kg.footprint.infrastructure.datasource.mapper.PostImageMapper">

    <insert id="insertImage" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO post_images (post_id, sort_order, storage_type, path, content_type, size_bytes, exif_available, created_at)
        VALUES (#{postId}, #{sortOrder}, #{storageType}, #{path}, #{contentType}, #{sizeBytes}, #{exifAvailable}, CURRENT_TIMESTAMP)
    </insert>

</mapper>